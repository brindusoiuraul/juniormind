using System;
using System.Net.Security;

namespace GzipAndCrypt
{
    public class GzipAndCrypt
    {
        public GzipAndCrypt()
        {
        }

        public void Write(Stream stream, string text, bool encrypt = false, bool compress = false)
        {
            throw new NotImplementedException();
        }

        public string Read(Stream stream, bool encrypt = false, bool compress = false)
        {
            throw new NotImplementedException();
        }
    }
}